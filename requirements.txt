# For an NVIDIA GPU, install torch/torchvision from the CUDA index instead:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
torch>=1.10
torchvision>=0.11
numpy
scikit-image>=0.19
matplotlib>=3.5
Pillow>=8.0        # JPEG / JPEG2000 baselines (baselines.py)
